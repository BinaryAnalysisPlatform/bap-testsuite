set test "stub-resolver"

set expected {
    "call @foo with"
    "call @bar@5b2 with"
}

set not_expected "call @bar with"
set file "dyn/x86_64-elf_so"

find_all $file bir $expected $test "--read-symbols-from data/x86_64-elf_so.symbols"

match $file bir "" {
    $not_expected {fail "$test for $call"}
}
